<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Movies
body
  p Movie: #{movie.title} in #{movie.year}
    if movie.imdb
      |, 
      a(href="http://www.imdb.com/title/#{movie.imdb}/") imdb
  a(href="/") back to list
