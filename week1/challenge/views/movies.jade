<!DOCTYPE html>
html(lang="en")
head
  meta(charset="UTF-8")
  title Movies
  style.
    .active {
      font-weight: bold;
      color: green;
    }
body
  ul
    each movie in movies
      li(class={active: movie._id == activeId}) #{movie.title}, #{movie.year}
  a(href="/add") add movie
